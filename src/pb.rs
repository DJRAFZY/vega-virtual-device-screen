//! Generated gRPC bindings for the Android-emulator control API
//! (package `android.emulation.control`, compiled from proto/emulator_controller.proto).
//!
//! Only `google.protobuf.Empty` is imported by the proto; prost maps it to `()`,
//! so the whole package fits in one flat module.
pub mod emu {
    tonic::include_proto!("android.emulation.control");
}
